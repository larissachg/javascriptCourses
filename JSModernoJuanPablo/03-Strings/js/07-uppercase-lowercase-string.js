const producto = 'Monitor 20 pulgadas';
console.log(producto);

console.warn('UpperCase');
//Para convertir un string a Mayusculas
console.log(producto.toUpperCase());

console.warn('LowerCase');
//Para convertir un string a Minuscula
console.log(producto.toLowerCase());

const email = "CORREO@CORREO.COM";
console.log(email.toLowerCase());

console.warn('toString');
//Para convertir un numero en un string
const precio = 300;
console.log(precio); //Los numeros se muestra de otro color
console.log(precio.toString());